<% if current_user.star?(restaurant) %>
  <%= button_to "Unstar", unstar_path(restaurant_id: restaurant.id), remote: true, method: :delete %>
<% else %>
  <%= button_to "Star", star_path(restaurant_id: restaurant.id), remote: true %>
<% end %>
